import 'package:flutter/material.dart';

class CreateExerciseGroupWidget extends StatelessWidget {
  const CreateExerciseGroupWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
